# AChat
Chat Java-Application

Release 1.0: 
  + Storing User data at Somee.com
  + Storing Image data (Avatar) at Cloudinary.com
  + Login and remember login session
  + Encrypt login data (Username, Password) with SHA256
  + AChat is only available with local area network (LAN)
  + Tested data transfer between real machine and virtual machine
  
 Release 1.0.1.1:
  + Fix error can write LocalUser.dat at C:\ProgramFile(x86)
  + Ask User before exit Program and delete PortID on Database
  
Release 1.0.2.1:
  + Fix error delte PortID when User exit Program
  + Add forgot password feature.
